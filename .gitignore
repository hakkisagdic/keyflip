# dependencies / build
node_modules/
*.tgz
coverage/
.nyc_output/

# macOS
.DS_Store

# editors
.vscode/
.idea/

# never commit local state / secrets
*.credentials
*.credentials.json
*.cred
.env
.env.*
*.pem
*.key
secrets*
!/src/secretscan.js

# license issuer: the Ed25519 PRIVATE key + runtime state (idempotency ledger,
# issued-license records) must NEVER be committed. Only the source + config +
# tests are tracked; the public key is pasted into src/license.js by hand.
issuer/private/
issuer/state/
